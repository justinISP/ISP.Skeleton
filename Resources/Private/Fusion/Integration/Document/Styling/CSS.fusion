prototype(ISP.Skeleton:CSS) < prototype(Neos.Fusion:Renderer) {
    renderPath = '/skeletonCss'
}

prototype(ISP.Skeleton:CSS.Renderer) < prototype(ISP.Skeleton:Presentation.CSS) {

    renderer = ${'<style>' + ISP.Skeleton.Minify.minifyCSS(props.styles) + '</style>'}

#    @cache {
#        mode = 'cached'
#        entryIdentifier {
#            static = 'footer'
#            site = ${site}
#        }
#        entryTags {
#            1 = ${Neos.Caching.nodeTypeTag('Neos.Neos:Document', site)}
#        }
#    }
}

skeletonCss = ISP.Skeleton:CSS.Renderer