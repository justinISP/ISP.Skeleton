prototype(Neos.NodeTypes:Headline) {

    anchor = ${q(node).property('anchor')}

    renderer = afx`
      <div {...props.attributes} id={props.anchor}>
        <Neos.Neos:Editable property={props.title} />
      </div>
    `
  }